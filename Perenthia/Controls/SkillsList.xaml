<UserControl x:Class="Perenthia.Controls.SkillsList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    Width="360" Height="Auto" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <Grid x:Name="LayoutRoot" Background="{x:Null}">
    	<ScrollViewer Height="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Style="{StaticResource ScrollViewerStyle}" Margin="0,20,0,0">
    		<StackPanel x:Name="list"/>
    	</ScrollViewer>
    	<TextBlock HorizontalAlignment="Right" Margin="0,0,26.186,0" VerticalAlignment="Top" Text="Skill Points:" x:Name="SkillPointLabel" TextWrapping="Wrap" d:LayoutOverrides="Width, Height" FontFamily="Trebuchet MS" Foreground="{StaticResource HeadingBrush}"/>
    	<TextBlock HorizontalAlignment="Right" VerticalAlignment="Top" Text="100" TextWrapping="Wrap" Width="22" x:Name="lblSkillPoints" FontFamily="Trebuchet MS" TextAlignment="Right" FontWeight="Bold" Foreground="{StaticResource PositiveBrush}"/>

    </Grid>
</UserControl>
