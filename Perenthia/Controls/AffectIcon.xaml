<UserControl x:Class="Perenthia.Controls.AffectIcon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    Width="22" Height="18">
    <Grid x:Name="LayoutRoot" Background="{x:Null}">
    	<Border Background="{StaticResource DialogFillBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,1,1,1" CornerRadius="2,2,2,2" Padding="1,1,1,1" Margin="2,0,2,0">
    		<Image x:Name="imgMain"/>
    	</Border>

    </Grid>
</UserControl>
